//category

const CATEGORY_CONFIG = {
    link: '/api/category/',
    perPage: 2,
    saveWith: 'categories',
    name: 'category'
}
const USER_CONFIG = {
    link: '/api/user/',
    perPage: "5",
    saveWith: 'users',
    name: 'user',
}
export {
    CATEGORY_CONFIG, USER_CONFIG
}